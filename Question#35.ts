const common = "can fly";

const animals: string[] = ["Eagle", "Parrot", "Sparrow"];

for (const animal of animals) {
  console.log(`${animal} ${common}.`);
}

console.log(`Any of these animals would make a great pet!`);
